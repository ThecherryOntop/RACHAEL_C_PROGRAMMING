#include <stdio.h>
// program to introduce for loop
int main(void)
{
    /*
    for (int i = 1, j = 2; i <= 5; ++i, j = j + 2){
    printf("%5d", i*j);
    }
// Program to introduce the while statement
int count = 1;

    while (count <= 5 ){
    printf("%d\n", count);
    ++count;
    }
*/
    int num = 0;
    scanf("%d", &num);

    while (num != 1){
       // loop action
        scanf("%d", &num);
    }
}


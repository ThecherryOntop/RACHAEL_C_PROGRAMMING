#include <stdio.h>
/**
 * main - Factorial
 * Description - Write a function to calculate the factorial
 * value of any integer entered through the keyboard
 *  factorial 
 * 
 */
int factorial(int x);
int main(void)
{
    int y;
    y = factorial(y);
    printf("%d", y);
}
int factorial(int x)
{
    int i;
    scanf("%d", &x);
    if (x > 0)
    {
        for ( x = 1; x <= x; x--)
        {
           x = x + x;
        }
        return (x);
         
    }
}

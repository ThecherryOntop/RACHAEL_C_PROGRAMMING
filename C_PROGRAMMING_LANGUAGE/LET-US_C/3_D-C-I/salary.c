#include <stdio.h>
#include <stdlib.h>
/**
 * Write a program to calculate the salary
 * 
 */
int main(void)
{
    int yos;
    int q;
    int G;
    
    printf("Enter gender, yos and qualification: ");
    scanf("%d %d %d", G, yos, q);
       if (yos >= 10 &&  && )
    printf("Salary: %d", 15000);
    else if (yos)
}